From 6e1524e8b0caedf01091e9fc9fb5b4435f5ea295 Mon Sep 17 00:00:00 2001
From: axfor <aixiaoxiang2009@hotmail.com>
Date: Mon, 21 Sep 2026 04:22:40 -0700
Subject: [PATCH 19/19] lib/streamaggr: dedup was losing every input series but
 one

An adversarial review found that the change which stopped building the input
part of a sample's key read aggrOutputs.useInputKey, and that field is already
false for a second, unrelated reason: dedup_interval > 0. The narrowing loop
that decides "no output reads the input part" only runs when dedup is off, so
the dedup case was never meant to be covered -- but compressLabels read the
same flag for both meanings.

With dedup on, dedupAggrShard keys its map on the now input-less key, so every
input series in an output group shares one entry. Three pods each reporting 1
through `interval: 1m / dedup_interval: 30s / without: [pod] / sum_samples`
summed to 1 instead of 3. total and rate got a single shared per-input state,
and count_series would always report 1.

The two questions are now two fields: useInputKey, untouched, and
buildInputKey, which is true unless dedup is off and no configured output
reads the input part. The test drives both dedup settings through the existing
stale-scenario harness; putting the shared flag back fails the dedup case.

Also from the same review:

- scrapework fed canStreamWithoutBody cb.SizeBytes(), the allocated capacity
  (chunks * 4096), where the maxScrapeSize check uses cb.Len(), the bytes read.
  Below a 4096-byte minResponseSizeForStreamParse the two paths disagreed about
  the same response. It uses Len now.

- -zstd.encoderConcurrency was declared only under !cgo, while the release
  vmagent is built with CGO_ENABLED=1, where passing it aborts start-up with
  "flag provided but not defined". It is declared for both builds and ignored
  by the cgo path, which compresses through gozstd and has no such pool.
---
 lib/encoding/zstd/concurrency.go | 20 ++++++++++++++++++++
 lib/encoding/zstd/zstd_pure.go   | 10 ----------
 lib/promscrape/scrapework.go     |  2 +-
 lib/streamaggr/input_key_test.go | 32 ++++++++++++++++++++++++++++++++
 lib/streamaggr/output.go         | 12 +++++++++---
 lib/streamaggr/streamaggr.go     |  6 +++++-
 6 files changed, 67 insertions(+), 15 deletions(-)
 create mode 100644 lib/encoding/zstd/concurrency.go

diff --git a/lib/encoding/zstd/concurrency.go b/lib/encoding/zstd/concurrency.go
new file mode 100644
index 000000000..c748ff98b
--- /dev/null
+++ b/lib/encoding/zstd/concurrency.go
@@ -0,0 +1,20 @@
+package zstd
+
+import "flag"
+
+// encoderConcurrency bounds how many blocks the pure-Go encoder may compress at
+// once. Each slot keeps an 8MB history window for the life of the process, and
+// the library defaults the count to GOMAXPROCS -- on a 64 CPU node that is
+// 512MB held whether or not there is anything to compress. Compressing a single
+// block does not use more than one slot; the concurrency only helps when
+// several blocks are compressed at the same time, so a process pushing a few
+// hundred KB/s has no use for more than one or two.
+//
+// It is declared for both builds so that passing it never aborts start-up. The
+// cgo build compresses through gozstd, which has no such pool, and ignores it.
+var encoderConcurrency = flag.Int("zstd.encoderConcurrency", 0, "How many blocks may be zstd-compressed at once. "+
+	"Each one keeps an 8MB history window for the life of the process, so this is also 8MB of memory apiece. "+
+	"0 means one per CPU, which is what the compression library defaults to. Compressing a single block does not "+
+	"use more than one of them -- the concurrency only helps when several blocks are compressed at the same time -- "+
+	"so a process that compresses a few hundred KB/s has no use for more than one or two. Has no effect in cgo "+
+	"builds, which compress through gozstd")
diff --git a/lib/encoding/zstd/zstd_pure.go b/lib/encoding/zstd/zstd_pure.go
index 09b425b11..735919702 100644
--- a/lib/encoding/zstd/zstd_pure.go
+++ b/lib/encoding/zstd/zstd_pure.go
@@ -3,7 +3,6 @@
 package zstd
 
 import (
-	"flag"
 	"runtime"
 	"sync"
 	"sync/atomic"
@@ -12,15 +11,6 @@ import (
 	"github.com/klauspost/compress/zstd"
 )
 
-var encoderConcurrency = flag.Int("zstd.encoderConcurrency", 0, "How many blocks may be zstd-compressed at once. "+
-	"Each one keeps an 8MB history window for the life of the process, so this is also 8MB of memory apiece. "+
-	"0 means one per CPU, which is what the compression library defaults to: on a 64 CPU node that is 512MB held "+
-	"whether or not there is anything to compress. Compressing a single block does not use more than one of them "+
-	"-- the concurrency only helps when several blocks are compressed at the same time -- so a process that "+
-	"compresses a few hundred KB/s has no use for more than one or two. Measured on one block at a time, "+
-	"concurrency makes no difference (4.2GB/s either way); with every goroutine compressing, throughput scales "+
-	"with it (4.2, 8.4, 16.6, 33.0 GB/s at 1, 2, 4 and 10)")
-
 var (
 	decoder *zstd.Decoder
 
diff --git a/lib/promscrape/scrapework.go b/lib/promscrape/scrapework.go
index 630ad042f..99d203447 100644
--- a/lib/promscrape/scrapework.go
+++ b/lib/promscrape/scrapework.go
@@ -467,7 +467,7 @@ func (sw *scrapeWork) scrapeInternal(scrapeTimestamp, realTimestamp int64) error
 	// without sacrificing the performance.
 	processScrapedDataConcurrencyLimitCh <- struct{}{}
 
-	if err == nil && sw.canStreamWithoutBody(cb.SizeBytes()) {
+	if err == nil && sw.canStreamWithoutBody(cb.Len()) {
 		// Decompress and parse the read response block by block, so the uncompressed response is never held in memory.
 		scrapesStreamedWithoutBody.Inc()
 		err = sw.processReadDataInStreamMode(scrapeTimestamp, realTimestamp, cb, contentEncoding, scrapeDurationSeconds)
diff --git a/lib/streamaggr/input_key_test.go b/lib/streamaggr/input_key_test.go
index 50ac1027c..8d834ba71 100644
--- a/lib/streamaggr/input_key_test.go
+++ b/lib/streamaggr/input_key_test.go
@@ -2,6 +2,7 @@ package streamaggr
 
 import (
 	"testing"
+	"time"
 )
 
 // The input part of a sample's key carries the labels the aggregation groups
@@ -79,3 +80,34 @@ func TestInputKeyIsKeptWhenAnyOutputNeedsIt(t *testing.T) {
 		})
 	}
 }
+
+// Leaving the input part out of a sample's key is only safe when nothing reads
+// it. With dedup_interval set, useInputKey is already false for a different
+// reason -- the deduplicator keys its own map on the full key -- so reading
+// that one flag to decide the key layout collapses every input series in an
+// output group onto a single sample, and the output reads as if only one pod
+// had reported.
+func TestDedupKeepsTheInputPartOfTheKey(t *testing.T) {
+	const threePods = `foo{pod="a"} 1
+foo{pod="b"} 1
+foo{pod="c"} 1`
+
+	for _, tc := range []struct {
+		name  string
+		dedup string
+	}{
+		{"dedup off", ""},
+		{"dedup on", "  dedup_interval: 30s\n"},
+	} {
+		t.Run(tc.name, func(t *testing.T) {
+			cfg := "- match: foo\n  interval: 1m\n" + tc.dedup + "  without: [pod]\n  outputs: [sum_samples]\n  keep_metric_names: true\n"
+			got := runStaleScenario(t, cfg, []pushAt{{at: 0, metrics: threePods}}, 2*time.Minute, 0)["foo"]
+			if len(got) == 0 {
+				t.Fatalf("no output samples")
+			}
+			if got[0].value != 3 {
+				t.Errorf("three pods each reporting 1 summed to %v, want 3 -- the input part of the key was dropped, so they shared one entry", got[0].value)
+			}
+		})
+	}
+}
diff --git a/lib/streamaggr/output.go b/lib/streamaggr/output.go
index c5909b485..7fa6750b8 100644
--- a/lib/streamaggr/output.go
+++ b/lib/streamaggr/output.go
@@ -10,9 +10,15 @@ import (
 )
 
 type aggrOutputs struct {
-	m                  sync.Map
-	useSharedState     bool
-	useInputKey        bool
+	m              sync.Map
+	useSharedState bool
+	useInputKey    bool
+	// buildInputKey is whether compressLabels puts the input part into a
+	// sample's key. It is not the same question as useInputKey, which is also
+	// false when dedup is on -- there the key layout still carries the input
+	// part, and the deduplicator keys its own map on it. Conflating the two
+	// collapses every input series in an output group onto one sample.
+	buildInputKey      bool
 	resetMarkerOnStale bool
 	configs            []aggrConfig
 	outputSamples      *metrics.Counter
diff --git a/lib/streamaggr/streamaggr.go b/lib/streamaggr/streamaggr.go
index bbe6c79d9..6e46b304a 100644
--- a/lib/streamaggr/streamaggr.go
+++ b/lib/streamaggr/streamaggr.go
@@ -613,6 +613,7 @@ func newAggregator(cfg *Config, path string, pushFunc PushFunc, ms *metrics.Set,
 		configs:            make([]aggrConfig, len(cfg.Outputs)),
 		useSharedState:     useSharedState,
 		useInputKey:        useInputKey,
+		buildInputKey:      true,
 		resetMarkerOnStale: cfg.ResetMarkerOnStale != nil && *cfg.ResetMarkerOnStale,
 	}
 	outputsSeen := make(map[string]struct{}, len(cfg.Outputs))
@@ -638,6 +639,9 @@ func newAggregator(cfg *Config, path string, pushFunc PushFunc, ms *metrics.Set,
 			}
 		}
 		aggrOutputs.useInputKey = useInputKey
+		// Only here may the input part be left out of the key: dedup off, and
+		// no configured output reads it.
+		aggrOutputs.buildInputKey = useInputKey
 	}
 
 	if aggrOutputs.resetMarkerOnStale && !aggrOutputs.hasCumulativeTotal() {
@@ -1014,7 +1018,7 @@ func (a *aggregator) Push(tss []prompb.TimeSeries, matchIdxs []byte) {
 	deleteDeadlineMsec := deleteDeadline.UnixMilli()
 
 	minDeadline := a.minDeadline.Load()
-	useInputKey := a.aggrOutputs.useInputKey
+	useInputKey := a.aggrOutputs.buildInputKey
 	dropLabels := a.dropInputLabels
 	ignoreOldSamples := a.ignoreOldSamples
 	enableWindows := a.enableWindows
-- 
2.50.1 (Apple Git-155)

